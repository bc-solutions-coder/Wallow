# Disable deprecated management metrics collection (deprecated in 3.13+).
# Prometheus-based metrics via the management plugin are used instead.
deprecated_features.permit.management_metrics_collection = false
